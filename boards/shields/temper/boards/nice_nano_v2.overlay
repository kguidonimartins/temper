/*
 * RGB underglow is disabled in temper.conf (CONFIG_ZMK_RGB_UNDERGLOW=n).
 * SPI3 + WS2812 were removed so no SPI peripheral stays initialized and fails
 * zmk_pm_suspend_devices() before sys_poweroff().
 *
 * Re-add the worldsemi,ws2812-spi block here if you enable underglow in Kconfig.
 */

/ {
};
